<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Data binding section -->
    <data>
        <import type="android.view.View" />
        <variable
            name="viewmodel"
            type="com.malenikajkat.classmate.ui.chats.ChatsViewModel" />
        <variable
            name="chatwithuserinfo"
            type="com.malenikajkat.classmate.data.model.ChatWithUserInfo" />
    </data>

    <!-- Main layout using ConstraintLayout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/cyan50"
        android:layout_marginStart="12dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="12dp"
        android:clickable="true"
        android:focusable="true"
        android:onClick="@{() -> viewmodel.selectChatWithUserInfoPressed(chatwithuserinfo)}">

        <!-- User profile image inside a CardView -->
        <androidx.cardview.widget.CardView
            android:id="@+id/imageCardView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="29dp"
            app:cardElevation="0dp"
            app:cardPreventCornerOverlap="false"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">
            <ImageView
                android:id="@+id/userProfileImage"
                android:layout_width="58dp"
                android:layout_height="58dp"
                android:layout_margin="0dp"
                android:contentDescription="@string/user_image"
                android:scaleType="centerCrop"
                tools:src="@tools:sample/avatars[0]" />
        </androidx.cardview.widget.CardView>

        <!-- Indicator for online status -->
        <View
            android:id="@+id/onlineView"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/round_circle_online_green"
            android:visibility="@{chatwithuserinfo.MUserInfo.online ? View.VISIBLE : View.INVISIBLE}"
            app:layout_constraintBottom_toBottomOf="@id/imageCardView"
            app:layout_constraintRight_toRightOf="@id/imageCardView"
            tools:visibility="visible" />

        <!-- Nested ConstraintLayout for text elements -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="3dp"
            android:layout_marginBottom="3dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@id/imageCardView"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- Display name text -->
            <TextView
                android:id="@+id/displayNameText"
                style="@style/BoldText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:singleLine="true"
                android:text="@{chatwithuserinfo.MUserInfo.displayName}"
                android:textSize="18sp"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toLeftOf="@id/timeText" />

            <!-- Last message text -->
            <TextView
                android:id="@+id/messageText"
                style="@style/RegularText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:singleLine="true"
                android:text="@{chatwithuserinfo.MChat.lastMessage}"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@id/displayNameText" />

            <!-- Time of the last message -->
            <TextView
                android:id="@+id/timeText"
                style="@style/RegularText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="@{chatwithuserinfo.MChat.lastMessageTime}"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="@id/displayNameText" />

            <!-- Indicator for unseen messages -->
            <View
                android:id="@+id/notSeenView"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/round_circle_unseen_indicator"
                android:visibility="@{viewmodel.myUserID == chatwithuserinfo.MChat.senderID ? View.INVISIBLE : View.VISIBLE}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                tools:visibility="visible" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>